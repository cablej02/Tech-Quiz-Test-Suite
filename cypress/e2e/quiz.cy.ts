describe('template spec', () => {
    // add the page to memory before each test
    beforeEach(() => {
        cy.visit('http://localhost:3000')
    })

    it('should get a new question on start game', () => {
    
    })

})